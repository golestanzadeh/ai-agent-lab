# Phase P1 continuous package — Synthetic Submission Readiness Dossier

Status: **COMPLETE / VERIFIED — EXTERNAL READINESS BLOCKED**

## Purpose and boundary

This package aggregates the exact synthetic Phase P1 lineage into one immutable,
privacy-minimized dossier. It does not claim production or external readiness and
contains no transmitter, official mapping, plausibility implementation, signing,
credential access, network call, real taxpayer value, or external receipt.

## Design

`src/agent_lab/elster_readiness_dossier.py` binds an exact ordered evidence set:

1. synthetic submission envelope;
2. versioned adapter contract;
3. completed synthetic material-process decision;
4. Human-readable synthetic preview;
5. lifecycle audit snapshot;
6. validated lifecycle recovery result.

The constructor verifies cross-artifact case, run, route, contract, preview,
snapshot, and idempotency lineage. Evidence references must be unique and appear in
the exact contract order. The dossier includes no tax amounts or purpose text.

Its only valid result is
`SYNTHETIC_DOSSIER_COMPLETE_EXTERNAL_READINESS_BLOCKED`. The immutable blocker set
records the three unrecovered official ERiC materials, absent official XML mapping
and plausibility implementation, unauthorized credentials/certificates, absent
transmitter, and unauthorized real ELSTER transmission.

## Verification evidence

- Targeted dossier suite: `19 passed`.
- Relevant Phase P1 suite: `123 passed`.
- Full repository regression: `505 passed, 1 skipped`.
- Python compile check: passed.
- Direct forging of evidence order, blockers, denied capabilities, privacy fields,
  or execution flags fails closed.

## Human Gate

The currently authorized local synthetic/non-production Phase P1 scope is now
implemented through a complete readiness dossier. Further P1 progress requires a
new exact Human authorization before developer registration, manufacturer-ID work,
protected official-material retrieval, or any later credential/external step.

Alternatively, beginning the separate user-interface phase requires its own exact
phase authorization. No action on either boundary has been taken.
