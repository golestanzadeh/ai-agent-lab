# UI package 8 — synthetic submission readiness

Status: **IMPLEMENTED / DISPLAY ONLY**

The view binds one synthetic case/run and immutable preview to two separate
Article 1 stages, both `NOT_APPROVED`, plus a closed set of official-material,
credential, transmitter, and authorization blockers. No action or transfer
capability exists.

Verification: targeted UI-8/app `19 passed`; full regression
`564 passed, 1 skipped`; loopback visual/accessibility inspection passed.
